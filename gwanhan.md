# 권한 총정리

> 기준 브랜치: `main`
> 작성일: 2026-09-13
> 이 문서는 지금까지 정한 모든 권한 관련 결정(기능별 매트릭스 + 프로젝트 생성/승인/삭제 거버넌스)을 하나로 합친 최종본입니다.

---

## 1. 기능별 권한 매트릭스

| 기능 | 관리자 | 조장 | 조원 |
|---|---|---|---|
| 프로젝트 생성 | ✅ | ✅ (승인 필요, 아래 2번 참고) | ❌ |
| 프로젝트 삭제 | ✅ | ❌ | ❌ |
| 프로젝트 정보 수정 | ✅ | ✅ | ❌ |
| 팀원 초대/제외(방출) | ✅ (모든 프로젝트) | ✅ (자기 프로젝트만) | ❌ |
| 팀장 위임 | ✅ | ✅ | ❌ |
| 과제 생성/수정/삭제 | ✅ | ✅ | ❌ |
| 과제 상태 변경(본인 담당) | ✅ | ✅ | ✅ |
| 파일 업로드 | ✅ | ✅ | ✅ |
| 정보 수집 실행 | ✅ | ✅ | ✅ |
| 동료평가 참여 | ❌ | ✅ | ✅ |
| 동료평가 결과 전체 열람 | ✅ | ❌ | ❌ |
| 팀 채팅(보기/쓰기) | ❌ | ✅ | ✅ |

> **업로드해주신 표와 달라진 부분**: "프로젝트 생성/삭제"가 원래 한 줄로 묶여서 조장 ❌였는데, 이후 논의를 거치며 **생성과 삭제가 서로 다른 권한으로 분리**됐어요 — 생성은 조장도 가능(단, 관리자 승인 전까지는 못 씀), 삭제는 여전히 관리자만. 나머지 행은 업로드하신 표와 동일해요.

---

## 2. 프로젝트 생성/승인/삭제 거버넌스 (위 표의 1~2번 행을 보충 설명)

이 부분은 단순히 "누가 버튼을 누를 수 있는가"를 넘어서, **평가 점수 조작을 막기 위한 절차**까지 포함하고 있어서 별도로 자세히 정리해요.

### 규칙
- 생성 시 **날짜(시작일·종료일) 필수** — 자유 텍스트 기간 입력은 제거됨
- **관리자가 만들면** → 바로 승인된 상태로 시작
- **조장이 만들면** → "승인 대기" 상태로 시작, 관리자가 `AdminPanel`에서 기간 등을 보고 승인/반려 판단 (서버가 자동으로 기간 길이를 검사해 거부하지는 않음 — 관리자 판단에 맡기기로 결정함)
- **승인 대기/반려 상태**에서는 대시보드 외 나머지 기능(팀 관리/채팅/과제보드/일정/워크스페이스/정보수집/동료평가) 접근 불가
- **수동 삭제**는 관리자만 가능하고, 삭제되면 데이터는 스냅샷 없이 즉시 사라짐
- **정상 종료 후 30일**이 지나면 스케줄러가 자동으로 정리하되, 정리 직전에 팀원별 평가 점수를 `evaluation_history`에 **영구 스냅샷**으로 남김 (계정 단위, 아무도 수정/삭제 불가)

### 이렇게 설계한 이유 (요약)
생성을 관리자만 가능하게 하면 조장의 자율성이 너무 줄고, 그렇다고 아무나 만들게 하면 "평가 라운드 리셋" 악용이 가능해요. 그래서 **생성은 열어주되 승인 절차로 걸러내고, 평가 기록은 프로젝트 생명주기와 분리해서 영구 보존**하는 쪽으로 정리했어요. 다만 "영구 보존"을 아무 때나 만들면 그 자체가 악용(좋은 점수 조작 후 굳히기) 경로가 될 수 있어서, **스냅샷은 오직 기간을 다 채우고 정상 종료했을 때만** 생기게 했어요.

---

## 3. 이 문서와 관련된 구현 파일

지금까지 만들어드린 코드/마이그레이션 파일들이 이 문서의 내용을 구현해요:

- `migration_admin.sql`, `migration_project_approval.sql`, `migration_creation_and_kick.sql`, `migration_require_dates.sql`, `migration_remove_duration_check.sql`, `migration_evaluation_history.sql`
- `AuthContext.tsx`, `AdminPanel.tsx`, `App.tsx`(승인 가드), `Dashboard.tsx`(승인 대기 안내), `CreateProjectModal.tsx`(날짜 필수)

(기능별 매트릭스의 나머지 항목 — 과제/채팅/파일/평가 등 — 은 각 컴포넌트의 RLS 정책에 이미 반영되어 있는지 별도 점검이 필요할 수 있어요. 특히 "동료평가 참여는 관리자 제외" 같은 규칙은 프론트엔드 UI에서만 막고 있을 가능성이 있어서, DB 레벨(RLS)에서도 막혀있는지 확인이 필요해요.)

---

## 4. 아직 안 다룬 것

- 기존(레거시) 자유 텍스트 기간 프로젝트의 날짜 보완
- 팀원 초대 절차에서의 악용 가능성 점검
- 동료평가 관련 규칙(참여 제한, 결과 열람 제한)이 실제로 DB RLS에도 반영되어 있는지 확인