# 개발용 로컬 PostgreSQL. docker compose -f server/docker-compose.yml up -d
# 접속 주소: postgres://postgres:postgres@localhost:5432/talju  (운영 비밀번호로 쓰지 말 것)
services:
  db:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: talju
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - talju-db:/var/lib/postgresql/data
volumes:
  talju-db:
